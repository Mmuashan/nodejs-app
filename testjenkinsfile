pipeline{
    agent any

    stages{
        stage('Dep install'){
            agent { docker { image 'node'}}
            steps{
                sh 'sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
            }
        }
    }
}